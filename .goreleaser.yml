version: 2

project_name: launch-pilot

before:
  hooks:
    - go test ./cmd/... ./internal/... ./web/...

builds:
  - main: ./cmd/launch-pilot
    binary: launch-pilot
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.Version={{.Version}}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

brews:
  - repository:
      owner: RoboZephyr
      name: homebrew-tap
    directory: Formula
    homepage: "https://github.com/RoboZephyr/launch-pilot"
    description: "Visual control console for macOS launchd services"
    license: MIT
    install: |
      bin.install "launch-pilot"
